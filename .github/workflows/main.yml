name: CI

on: [push]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: create all containers 
      run: |
        docker-compose -f "docker-compose.yml" up -d --build


